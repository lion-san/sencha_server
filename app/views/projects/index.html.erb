<% provide(:title, 'Project list') %>
<h1>Projects belong "test" user</h1>

<ui>

<% @projects.each do |p| %>

  <li>
    <strong>
    <%= link_to p.pjname, p, medhod: :show %>
    </strong>
  | <%= link_to "delete", p, method: :delete,
                   data: { confirm: "You sure?" } %>
  </li>


<% end %>

</ui>
